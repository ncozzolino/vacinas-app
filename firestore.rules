rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Cada usuário só lê/escreve o documento da própria criança (doc id = uid).
    match /criancas/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
